@{
        Layout = "_Layout";
    }
@*@{
        var cookie = Context.Request.Cookies;
        if (cookie.Count > 0) // or other custom logic
        {
            Layout = "~/Views/Shared/AfterLoginLayout.cshtml";
        }
        else
        {
            Layout = "~/Views/Shared/_Layout.cshtml";
        }
    }*@
